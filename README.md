# ruby_project
Selenium and Ruby
Steps to set up project:
Run bundle insatll //First create Gemfile folder and add the source from where the gems need to be insatlled
(or)
Install the following Ruby gems
        gem install selenium-webdriver
        gem install rspec
Run test
        rspec login_spec.rb
